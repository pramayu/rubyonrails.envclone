<%= simple_form_for([@theme, @theme.comments.build], remote: true )do |f| %>
	<div class="form-group txt-ttt">
		<%= f.input :content, class: "form-control comment-frm blupblup", label: false, placeholder: "Add Comment" %>
	</div>
	<%= f.submit "Comment", class: "btn btn-default btn-oi" %>
<% end %>